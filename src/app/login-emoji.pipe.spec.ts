import { LoginEmojiPipe } from './login-emoji.pipe';

describe('LoginEmojiPipe', () => {
  it('create an instance', () => {
    const pipe = new LoginEmojiPipe();
    expect(pipe).toBeTruthy();
  });
});
